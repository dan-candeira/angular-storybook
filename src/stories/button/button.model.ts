export type Variant = 'primary' | 'secondary' | 'tertiary';
export type Size = 'small' | 'medium' | 'large';
export type Type = 'submit' | 'button' | 'reset';
export type Position = 'after' | 'before';

export enum StyleType {
	iconOnly = 'iconOnly',
	common = 'common',
}

export enum IconPostition {
	after = 'after',
	before = 'before',
}
